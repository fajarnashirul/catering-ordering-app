<h1>Menu#index</h1>
<p>Find me in app/views/menu/index.html.erb</p>
    <% @menus.each do |menu| %>
      <div class="posts-index-item">
        <%= menu.name %>
        menu.description
        menu.price
        <%= link_to('gass', "/menu/#{menu.id}") %>
      </div>
    <% end %>
